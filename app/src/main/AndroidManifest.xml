<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhirova.alina.beautystyle">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TopActvity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".nails.NailCategoryActivity" />
        <activity android:name=".nails.NailActivity" />
        <activity android:name=".hair.HairCategoryActivity"></activity>
        <activity android:name=".hair.HairActivity"></activity>
        <activity android:name=".location.LocationCategoryActivity"></activity>
    </application>

</manifest>